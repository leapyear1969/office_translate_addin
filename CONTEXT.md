# 翻译插件使用统计

本词汇表定义 Word / Outlook 翻译插件使用统计中的业务概念，区分个人行为分析与服务运维、用量统计。

## Language

**管理后台**：
用于查看授权范围内的用户使用、API 用量、参考消耗金额及故障数据的独立浏览器网站，不要求管理员先进入 Office 插件。
_Avoid_：翻译设置面板、Office 内管理员面板、Graph 同意页面

**管理网页登录**：
管理员在普通浏览器中证明账号身份的登录过程，不依赖 Office 插件环境；登录成功本身不授予后台数据权限，仍须满足本平台的管理员授权规则。
_Avoid_：Graph 权限同意、登录成功即有管理权限

**后台管理员**：
由本平台明确授权可查询指定数据范围的账号，第一版仅具备后台查询资格，不自动获得统计数据删除或价格变更等维护权限；不因是已登录用户或拥有某个 Entra 租户的管理身份而自动获得本系统后台权限。
_Avoid_：所有已登录用户、自动继承的 Entra 租户管理员

**无可验证租户故障数据**：
服务端已观测到、属于插件用户流程，但缺少可靠证据归属具体租户的故障或错误观测数据；它不属于任意一个被查询租户，不能依据未验证的身份声明或报告来源猜测归属。其访问受独立服务级只读授权控制，租户查询资格不自动包含此范围，也不等同于故障关联信息缺失，不自动包含统计与管理系统的健康日志。
_Avoid_：任意租户的故障、未关联故障观测、未验证的租户声明即可靠归属

**服务级故障查询权限**：
明确授予指定后台账号读取无可验证租户故障汇总和限额样本的只读资格，与已验证租户的数据查询范围分别授权。它不自动授予任何已验证租户的数据访问权，也不包含统计维护权限。
_Avoid_：跨租户查询资格自动附带、所有后台管理员默认可见、统计维护权限

**统计维护**：
执行统计用户数据删除或价格配置变更等会改变保有数据或报表结果的特权操作，不等同于查看后台报表。维护授权独立于后台查询资格，不因进入管理员白名单或拥有身份目录管理权限而自动获得。
_Avoid_：后台只读查询、所有后台管理员均可维护、身份目录管理

**翻译范围**：
翻译任务所针对的内容范围，例如 Word 选区、当前段落、文档正文，或 Outlook 邮件正文、手动文本；它描述翻译什么，不描述译文如何呈现。
_Avoid_：预览、交付方式、把选区与预览视为互斥范围

**交付方式**：
翻译任务预期采用的译文呈现方式，例如面板预览、当前文档正文写回、译文副本、临时替换邮件显示；不代表交付已经成功，复制和插入预览结果仍是另行统计的结果使用行为。
_Avoid_：翻译范围、复制次数、插入次数、交付方式已确定即交付成功

**服务商计量**：
翻译服务针对实际请求明确返回的可信用量，优先作为该请求的计价用量依据；它不是实际应付金额，缺少计量值也不等同于零用量。
_Avoid_：原文长度、提交量、账单实付、缺失即零

**本地计量估算**：
缺少服务商计量时，依据已核实且经过验证的适用规则，对实际请求用量进行的本地估算；可用于参考消耗金额，但必须与服务商计量区分，规则不足以合理确定用量时仍为未知。
_Avoid_：服务商实测计量、直接套用未经核实的字符串长度

**已确认免计量**：
适用规则已明确确认不消耗相应计价用量的情况，不等同于没有取得计量值；接口仍可能有实际请求、提交量和运维消耗。
_Avoid_：计量未知、没有请求、缺少计量头

**参考消耗金额**：
按适用计量规则确定的可计价用量，经计价单位换算后乘以覆盖请求发生时间的当前有效配置单价得到的参考金额；补录或纠正价格后，可按其生效期间更新仍有充分计量依据的历史金额，不改写实际请求或用量事实。它不模拟资源级免费额度、包量、阶梯折扣或用户间分摊，也不等同于服务商账单实付或不可更改的结算账本。
_Avoid_：实际应付费用、账单金额、套餐分摊成本

**翻译业务请求**：
客户端向插件后端发起的一次语言检测或翻译请求，包括未产生翻译操作的自动预检请求；不包含登录资料查询、统计上报或管理查询，也不等同于一次翻译操作。
_Avoid_：所有后端 API 流量、翻译操作数、上游翻译服务请求

**上游翻译服务请求**：
后端实际向语言检测或翻译服务发起的一次请求尝试，一次翻译业务请求可以产生多次此类请求；身份认证和用户资料获取等调用不在此统计范围内。
_Avoid_：所有外部服务请求、翻译业务请求数、按钮点击数

**上游翻译请求成功**：
翻译服务正常完成请求并返回符合约定响应结构的结果；不代表其中的译文已通过 Word 的结构映射校验，也不代表已经向用户成功交付。
_Avoid_：译文可安全写回、翻译操作成功、成功使用

**用户流程故障**：
Word / Outlook 插件供用户使用的登录、自动预检、读取、翻译、结果交付、复制、插入或恢复等流程中的技术故障，是质量页主故障指标的统计范围。它不要求已经形成翻译操作，也不包含仅发生在统计采集、管理网站或统计维护流程中的故障。
_Avoid_：只有最终失败操作才有故障、所有后端错误、统计采集故障即翻译失败

**统计与管理系统故障**：
发生在遥测采集、统计存储、管理网站登录或查询、统计维护等支持流程中的技术问题，与插件用户流程故障分开处理。它可导致统计覆盖不足或后台不可用，但本身不代表翻译失败，不进入插件主故障数或生成翻译操作。
_Avoid_：插件用户流程故障、用户未使用、额外失败翻译操作

**故障发生**：
一次实际执行中发生的独立技术异常，同一次异常在调用链上的传播或重复报告不增加发生次数；另一次请求尝试或后续独立处理再次失败，则是新的故障，即使属于同一翻译操作或具有相同错误码。它不同于失败操作、失败请求或错误报告的计数，翻译安全限制与无待译内容不属于技术故障。
_Avoid_：错误报告条数、各层失败次数相加、同一操作最多一个故障、同一错误码只算一次

**故障传播**：
同一次已发生异常被后续处理环节接收、包装或报告的过程，不表示每个报告环节又发生了独立故障。只有可靠关联到同一次异常时才能合并，不能仅凭错误码、用户或发生时间接近推断。
_Avoid_：每个传播层都算新故障、修复或重试过程中新的独立失败

**未关联故障观测**：
已收到错误报告，但缺乏可靠证据判断它是另一独立故障还是已有故障的传播，不等同于已确认的故障发生，也不表示用户或租户身份未知。它按去重后的观测单独计数，不丢弃、不直接加进独立故障总数，也不凭错误码相同或时间接近强行合并。
_Avoid_：已确认独立故障、无身份故障、每份报告必定一个新故障、未关联即无错误

**故障样本**：
从已确认故障或未关联故障观测中按限额保留、供排查使用的明细，须明确区分类别，不保证每个用户都有样本，也不能用样本条数代替故障发生或观测计数。没有保留样本不等于没有故障，样本也不等同于翻译操作的结果记录。
_Avoid_：每用户保底明细、完整故障日志、故障总次数、失败操作状态

**译文结构校验问题**：
译文标记、顺序或片段映射不满足 Word 安全处理要求的业务问题，可发生在正常完成的上游翻译请求之后；它不是对译文语义准确性的质量评价。
_Avoid_：上游 HTTP 请求失败、原文因安全限制不予翻译、译文准确率

**上游请求重试**：
某次上游请求失败后，为再次完成该请求而发起的请求尝试；正常内容分批以及为修复已返回业务结果而改用处理路径的重译，不因此成为请求重试。
_Avoid_：正常分批、修复性重译、用户重新发起的独立翻译操作

**修复性重译**：
已返回的译文未满足既有业务校验要求时，为修复结果而再次翻译的处理阶段，例如 Word 标记不匹配或命中既有疑似未翻译规则后的重译；该阶段可以包含多个实际上游请求，不等同于一次请求重试。
_Avoid_：网络重试、每次修复必定只调用一次 API

**自动预检**：
插件为决定是否需要翻译而自动读取、检测内容或提出翻译询问的检查过程，包括新操作开始前的 Word 自动内容检查与 Outlook 语言检查，不等同于已经发起翻译；自动检查发现空内容、无需翻译或尚未获得用户确认时，不构成翻译操作或有效使用。其实际检测服务用量和技术故障仍是可统计的服务事实，不能与用户主动请求翻译后得到的无待译内容结果混淆。
_Avoid_：翻译操作、翻译成功、有效使用、CORS 预检

**翻译操作**：
用户明确发起翻译，或系统已决定执行翻译后形成的一次逻辑任务；不要求已经调用翻译 API，读取等前置处理失败仍可能是该任务的失败。决定是否翻译的自动预检本身不属于翻译操作。
_Avoid_：每次 API 请求、自动预检、只有调用翻译 API 才算操作

**手动触发**：
翻译直接由用户明确针对本次翻译的动作发起，包括提交待译文字、点击翻译或确认翻译；动作后采用防抖、定时或异步处理，不改变其手动性质。
_Avoid_：仅翻译按钮才算手动、用了防抖就算自动

**自动触发**：
没有用户明确针对本次翻译的新请求，而由既有设置或宿主事件自行启动翻译，例如按始终翻译设置处理邮件、跟随普通 Word 选区变化翻译；用户切换邮件或普通选区本身不等同于明确要求本次翻译。
_Avoid_：所有异步调用、自动询问后用户确认翻译、用户主动提交待译文字

**翻译入口**：
直接发起本次翻译的控件、命令或事件所在入口，不等同于最初打开面板的来源；经右键打开面板后再在面板中发起翻译，翻译入口属于面板。
_Avoid_：面板打开来源、最早打开动作的长期归因

**面板打开来源**：
直接导致面板打开的入口来源，与翻译入口分别描述不同的行为；无法可靠识别时为未知，不根据后续翻译或历史操作猜测。
_Avoid_：翻译入口、由面板内操作反推打开来源

**设置变更**：
用户明确修改设置并实际生效的改变，不包含初始化读取、切换文档或自动回填产生的观测差异；同值重复设置不构成变更。最新设置观测可以更新而不发生此类变更，记录设置变更也不使该行为自动成为有效使用。
_Avoid_：最新快照不同即用户修改、初始化算变更、同值重复设置、未生效的修改尝试

**设置修改次数**：
用户一次明确修改或保存提交实际生效、且至少一个设置项发生变化时计一次；一次修改涉及多个设置项仍只计一次，没有实际变化不计。它是独立的设置行为计数，不是翻译操作数，也不能与设置项变更次数相加。
_Avoid_：保存按钮点击数、设置项数量、翻译操作数

**设置项变更次数**：
按设置项分别累计用户修改并实际生效的变化次数；同一次修改涉及两项时两项各计一次，列表型设置在同次修改中改变多个列表内容仍属于该设置项的一次变化。各项合计描述字段变化量，不代表用户提交了多少次修改。
_Avoid_：设置修改次数、列表中每种语言都算一个设置项、观测快照变化次数

**最新观测设置分布**：
以所选期间及适用筛选条件内发生过有效使用的用户为人群，按宿主查看其最近观测设置值的分布；观测时间可以晚于所选期间，不是历史期末快照，也不表示用户全部文档的全局偏好。它描述该人群最新已观测到的设置，未观测不等于默认值，也不能用当前值推断过去的设置变更。
_Avoid_：历史期末设置快照、全体账号当前偏好、缺失即默认设置、其他宿主的设置替代

**个人行为分析**：
以可识别用户为对象，对其活跃、留存、功能习惯和最近使用情况进行的分析，不包含仅用于成本与运维的 API 用量和必要故障统计。
_Avoid_：将服务端可识别的全部使用记录统称为个人行为分析

**服务运维与用量统计**：
用于服务可靠性排查和成本核算、可关联真实用户身份的 API 用量及必要故障统计；与个人行为分析的衡量目的不同，不等同于完整的用户行为记录。
_Avoid_：匿名统计、完整行为记录

**统计用户数据删除**：
清除指定用户在在线统计库中的个人资料及可关联使用数据，包括个人维度的明细、汇总和偏好，并同步移除其对派生报表的贡献；不通过另存匿名总量继续保留这些贡献，因此历史人数、操作数、用量及参考金额可以减少，仅允许例外保留防止旧数据重新入库所必需的最小删除执行记录。它不等同于删除身份目录账号、退出登录、关闭未来统计采集或清理 Word 的文档原文备份。
_Avoid_：仅删除姓名邮箱、删除身份目录账号、统计关闭开关、历史汇总必定不变

**删除执行记录**：
为防止已删除的历史使用数据因迟到上报重新出现而保留的最小执行凭据，标识已清理的统计用户及删除时间边界；它仍可关联个人，但不是行为记录、首次使用档案或永久停止采集标记。仅用于落实删除边界，相关旧数据已不能再被合法接收或回补后应清理，不用于活跃、留存或新老用户分析。
_Avoid_：匿名记录、用户画像、永久禁采名单、事件去重回执

**有效使用**：
发起翻译或预览、触发且实际进入处理的新预览刷新轮次、取得成功或部分成功结果，或成功复制、插入、查看原文、恢复原文的使用行为；不包含单纯打开或闲置面板、登录刷新、自动预检、未执行的输入变化，也不把内部重试或修复请求本身另算使用。实际处理的新轮次即使失败也按其触发日期构成有效使用，但不新增翻译操作；同一操作可涉及多个使用日，同一用户同日仍只计一个用户日，成功使用仍属于有效使用。
_Avoid_：仅发起操作才算使用、面板在线即活跃

**期间活跃**：
在所选日期范围及其他筛选条件内至少发生一次有效使用的去重账号人数；同一账号跨日、跨周使用仍只计一人，不等同于周活跃的累加、周平均值或最后一周人数。
_Avoid_：将跨周期间人数称为周活跃、日活相加、周活相加

**期间成功使用**：
在所选日期范围及其他筛选条件内至少发生一次成功使用的去重账号人数，包含取得部分成功结果的用户；按成功使用事实的实际发生日期认定，不以操作发起日归属的成功操作数代替。
_Avoid_：成功操作数、周成功使用相加、仅完整成功用户

**周活跃**：
按自然周分组、在该周的所选日期内发生有效使用的去重账号人数；选定日期只覆盖部分周时，该值是部分周人数，不代表完整自然周，也不是整个跨周期限的人数。
_Avoid_：期间活跃、滚动七天人数、不同周人数相加

**周成功使用**：
按自然周分组、在该周的所选日期内至少发生一次成功使用的去重账号人数，包含部分成功；部分周值须与完整周区分，不等同于整个期间的成功使用人数。
_Avoid_：期间成功使用、成功操作数、仅完整成功用户

**成功操作**：
请求范围内需要翻译的内容均已完成，并已确认有效译文在可见预览中呈现、当前有效预览结果被成功复制／插入，或在 Office 中显示、写回的一次翻译操作；不包含部分成功。成功复制／插入可作为独立的完成依据，但不用于伪造此前的预览可见交付事实。
_Avoid_：成功使用、API 请求成功即操作成功

**成功使用**：
翻译取得成功或部分成功结果、预览发生成功交付，或成功复制、插入译文的使用行为；表示已确认交付或使用译文，不表示译文准确性已经验证。翻译、预览按实际展示或写回完成时间认定成功使用，复制、插入按各自实际发生时间认定，不回溯到翻译发起时间，也不因预览后续刷新失败而撤销已发生的成功使用。
_Avoid_：仅完整成功的操作、翻译质量合格、以预览最终状态覆盖历史成功使用

**预览操作**：
围绕一个预览范围持续生成、修订译文的翻译任务，不等同于其中的单轮自动刷新；成功复制或插入结果是当前任务的一个结束点，但不要求关闭界面或清空译文，暂时隐藏窗格或同一范围的选区暂时变空也不立即结束尚未结束的任务，空闲结束前恢复有内容时仍可继续。结束后重复复制同一结果仅属于结果使用，之后再次编辑或重新翻译才形成新的预览操作；失败的复制、插入不构成任务完成。
_Avoid_：每次自动刷新、每次复制、面板打开次数、隐藏窗格即取消

**取消**：
用户明确终止尚未完成的翻译处理，或明确离开、替换当前预览任务而放弃其未完成处理的结果；不代表上游请求已经实际中止，也不抹掉已发生的交付、用量或故障。当前已经确定的成功、部分成功、失败、受限或无待译内容，不仅因换任务就改成取消，隐藏窗格或缺少结果报告也不能据以推断取消。
_Avoid_：范围切换一律取消、隐藏窗格、结果未知、上游请求必定已中止

**结果未知**：
已登记的翻译操作在最后一次可观测活动后超过等待期限，仍未获得符合接收规则的最终结果的暂定状态；它不等同于明确失败或用户取消，也不否定已确认的历史成功使用。因时间不可信等原因被拒收的报告不能据以确认终态，后续收到有效终态可修正，单纯重复收到旧记录不构成新的操作活动。
_Avoid_：翻译失败、超过固定操作时长即失联、重复上报即持续活跃

**预览空闲结束**：
预览长时间没有新的翻译相关活动，且没有待执行或在途的相关处理时，依据当前范围与结果结束任务统计的边界；当前范围为空可结束为无待译内容，当前结果就绪但无展示或成功使用确认可结束为未确认展示结束，不等同于用户取消或关页，也不撤销历史成功使用。单纯自动空检查不延长期限，空闲结束不要求关闭界面，其发生时刻不代表译文刚刚完成或交付。
_Avoid_：用户取消、空闲时间即翻译耗时、后端凭历史交付猜测成功

**预览刷新轮次**：
针对一组确定的预览输入和目标语言触发的一轮翻译处理，可能包含多个 API 请求，一个预览操作可以包含多轮刷新；同轮内部的分批、重试或修复不形成新轮次。实际进入处理的新轮次按触发时间构成有效使用，即使失败或后来过期也不撤销该使用事实，但过期或未实际展示不构成成功交付；未执行就被替代的输入变化不作为已处理轮次统计。
_Avoid_：一次上游 API 请求、一次预览操作

**操作目标语言**：
一次预览操作最后一轮翻译所请求的目标语言，是该操作计数与结果的语言归属；即使最后一轮失败也不退回此前成功轮次的语言。它不覆盖历史刷新轮次、API 请求或成功交付各自当时的目标语言。
_Avoid_：最后成功译文的语言、整次操作一律归多目标语言、最后收到的上报语言

**操作源语言**：
预览操作最后一轮输入对应的已识别源语言，须有与该输入匹配的可信依据，并与最后一轮目标语言共同描述操作的语言归属；该轮翻译失败不否定已经取得的适用识别结果，没有依据时为未知，不退回旧输入的语言。只有该轮本身确有多个已识别源语言时才归为多语言，跨轮更换输入语言不因此成为多语言操作，也不覆盖历史请求或交付各自的语言。
_Avoid_：最早输入的语言、最后收到的识别结果、跨轮出现过的语言集合、翻译失败即源语言未知

**预览刷新等待耗时**：
从某轮预览触发翻译处理，到该轮有效译文实际展示的等待时间；以有效交付的刷新轮次为成功耗时样本，不以整次预览的持续时间或首次结果后的阅读、闲置时间代替。
_Avoid_：预览会话时长、只看首次结果的耗时、上游 API 耗时

**预览就绪**：
与当前输入和目标语言匹配、有效且非过期的译文已经写入预览区域并可供使用，但未必已经在可见预览中呈现。后台就绪本身不构成预览成功交付或成功使用，也不能单独证明整次操作已经成功。
_Avoid_：预览成功交付、用户已经看到译文、仅 API 返回成功

**预览成功交付**：
能够可靠确认宿主窗格及当前预览视图可见时，与当时输入和目标语言匹配的有效、非过期译文已呈现的交付事实；后台就绪不等于交付，交付也不代表用户已阅读或理解译文。每轮首次确认交付形成成功使用，隐藏后重开同一已交付结果不重复计数，后续输入变化或失败不撤销历史交付，也不表示整次预览操作当时已经结束。
_Avoid_：API 成功但未展示、过期响应、整次预览操作成功

**可观测使用历史**：
统计系统当前保有的某账号使用事实及产品级首用认定依据；完整删除这些数据，或连同首用依据一并不可恢复丢失后，返回使用会重新建立此历史，可能再次形成新增用户及新的留存首周，不表示用户第一次使用产品。筛选变化、重新登录、服务重启或普通明细到期清理不重置仍保有依据的使用历史，不借助删除执行记录或额外长期首用档案追认已清理历史。
_Avoid_：用户真实终身使用历史、查询日期范围、删除执行记录、每次登录一个新用户

**首次有效使用**：
同一租户、同一账号在当前可观测使用历史中，整个 Word / Outlook 翻译插件最早一次可观测的有效使用，跨宿主、功能和版本统一认定；改变筛选范围不会重新产生首次有效使用，不推断已被清理或未被记录的历史使用。
_Avoid_：首次使用当前筛选功能、安装或注册时间

**新增用户**：
当前可观测使用历史中的产品级首次有效使用发生在所选日期范围内的用户；完整删除或首用依据不可恢复丢失后重建历史，可以再次被计入，不表示用户首次使用产品。仍保有首用依据的老用户首次使用另一宿主、功能或版本，不因此再次成为新增用户。
_Avoid_：新增功能用户、安装用户、注册用户

**首次成功使用**：
同一租户、同一账号在当前可观测使用历史中，整个 Word / Outlook 翻译插件最早一次可观测的成功使用，跨宿主、功能和版本统一认定，不等同于首次发起翻译，也不随筛选范围重置。跨周翻译以实际成功交付译文所在周作为首次成功使用周，延迟收到记录不会改变这一事实时间；完整删除或首用依据不可恢复丢失后可随历史重建重新认定，不推断已被清理或未被记录的历史使用。
_Avoid_：首次发起翻译、首次收到成功上报、首次使用当前筛选功能

**留存首周**：
用户当前可观测使用历史中产品级首次成功使用所在的自然周，是次周留存分组的时间依据；留存的日期筛选选择这一首周，而不是次周观察周，完整删除或首用依据不可恢复丢失后重建历史可形成新的首周。
_Avoid_：首次发起翻译周、首次使用当前筛选功能周、次周观察周

**部分首周样本**：
日期筛选只覆盖某自然周的一部分时，该周内产品级首次成功使用日期落在所选范围内、并符合当前非日期筛选条件的留存人群；不自动纳入未选日期的首次成功用户，仍观察下一完整自然周。
_Avoid_：整周全部新增人群、部分次周观察

**次周观察周**：
留存首周之后紧接的完整自然周，用于判断首周用户是否在相同非日期筛选范围内再次成功使用；该周尚未结束时，留存仍处于观察中。
_Avoid_：首用后滚动七天、留存首周

**次周留存**：
当前可观测使用历史中的产品级首次成功使用发生在某自然周、且该周在当前非日期筛选范围内成功使用过的用户中，下一自然周仍在同一范围成功使用的用户比例；筛选不重置产品首次成功使用周。不限定宿主时跨 Word / Outlook 的再次成功使用可构成产品留存，限定宿主时则必须在同一宿主再次成功使用。
_Avoid_：首次使用某功能后的留存、筛选只限定首周人群的产品留存

**成熟留存分组**：
对应的次周观察周已经完整结束的留存首周人群；成熟只表示观察时间已结束，不保证采集完整，也不表示数据已经冻结。汇总留存只纳入有首周样本的成熟分组，尚未成熟的人群单独显示观察状态。
_Avoid_：七天迟到窗口也已结束、记录不能再修正、部分次周观察已算成熟

**汇总次周留存**：
对所选首周样本中的成熟分组，合并满足相同非日期筛选条件的次周留存人数与首周样本人数后计算的比例；未成熟分组不进入分子或分母，各首周的明细比例仍独立保留。它不是各周留存率的等权平均，没有成熟样本时不能解释为零留存。
_Avoid_：各周百分比直接平均、未成熟人群当作流失、期间成功使用人数作为分母

**部分成功**：
一次翻译操作中，已确认完成部分请求范围内的正文翻译，但仍有应翻译正文未完成；未完成可来自翻译安全限制或执行异常，不包含空白等无需翻译的内容。
_Avoid_：程序正常结束即成功、有任何跳过即技术故障

**无待译内容**：
用户主动发起翻译后，正常读取并确认请求范围为空或仅含无需翻译的空白，或已有预览在当前范围确认仍为空且无待执行或在途处理时按既定边界结束的明确结果；保留已发起操作及其有效使用，但该结果本身不构成成功使用、技术故障或翻译安全限制。它计入结果覆盖，不参与端到端成功率计算，也不否定同次预览此前已确认的成功交付；未形成翻译操作的自动空检查不属于此结果。
_Avoid_：翻译失败、翻译受限、用户取消、读取失败、存在待译内容但未能安全处理

**未确认展示结束**：
预览在当前有效译文已就绪、尚无该结果可见交付或成功复制／插入的确认，且没有待执行或在途处理时，按既定边界结束统计的非故障结果；描述的是结束时当前结果的确认情况，不断言用户实际从未看过，也不否定此前其他结果的成功使用。它计入结果覆盖但不进入端到端成功率等共同分母，之后才发生的展示或使用属于新的独立事实，不重开该操作，也不因此将原终态改成成功。
_Avoid_：翻译失败、用户取消、结果缺报、整个操作从未成功使用、后台仅凭 API 成功推断终态

**翻译安全限制**：
为避免破坏 Word 文档内容或结构，而对不支持安全处理的正文保留原文或停止翻译的产品保护边界；因此未完成的翻译需求不等同于技术故障。
_Avoid_：翻译服务故障、无需翻译的内容

**翻译受限**：
一次翻译操作因翻译安全限制而未能完成任何请求范围内的正文翻译、未交付有效译文的结果；它表示翻译需求未完成，不等同于技术故障或用户主动取消。
_Avoid_：部分成功、技术故障、用户取消
