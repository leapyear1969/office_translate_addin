# 验收清单

## 已可自动验证
- JWT 签名、受众、租户、过期、access_as_user 权限检查。
- 未认证请求不会调用翻译；外部 Origin 和非法输入被拒绝。
- 全文翻译保留表格、图片、URL、样式、字符转义和行内空格。
- 大邮件按限制分批；服务失败保留原文；邮件切换不写入错误邮件。
- 默认设置、忽略语言、保存和保存失败回滚。
- TypeScript、Webpack 和 Office manifest schema 校验。

## 需要真实 Outlook 与应用配置
- 填好 CLIENT_SECRET，确认资源 TRANSLATOR_REGION，重启服务。
- Entra 已公开 access_as_user、预授权 Office、授权 Graph User.Read。
- 重新加载 manifest.xml，菜单仅包含“翻译邮件”和“翻译选项”。
- 打开翻译选项，SSO 正确显示当前用户姓名、邮箱；切换账户后不能显示旧用户信息。
- 选择目标语言，添加/删除排除语言，保存并重开面板确认持久化。
- 点击翻译邮件，正文显示实际译文，表格、图片和链接保留；不修改服务器邮件。
- 翻译中切换邮件，不把旧邮件译文显示到新邮件。
- 翻译完成后，在提示栏的“关闭”前可见“显示原文”；点击后打开翻译选项并自动恢复原文，面板保持打开，不再跳转到独立原文页面。
- 翻译选项顶部包含“显示原文”和“将邮件翻译为：中文（简体）”按钮；后者显示已保存的目标语言。更改并保存目标语言后，按钮文字及实际翻译语言同步更新；“从不自动翻译”仍允许手动翻译。
- 不支持阅读模式提示栏按钮的客户端，提示打开“翻译选项”使用其中的“显示原文”按钮。
- 保持翻译选项面板打开，再次翻译并点击提示栏“显示原文”，确认仍能恢复；切换邮件期间不将上一封原文写入新邮件。
- “始终翻译”模式下，从提示栏或选项面板恢复原文后，不立刻自动翻译覆盖原文；切换下一封邮件后恢复自动翻译行为。
- 重新打开邮件仍恢复原文；恢复原文不触发登录或翻译请求。
- 固定面板，确认 always/ask/never 三种行为及排除语言；关闭面板不会自动翻译。
- SSO 或 Azure Key/区域错误时显示可操作错误提示并保留原文。
- 多语言/多节点邮件需人工评估译文连贯性；附件、主题、图片文字保持原样。

## 需要真实 Word 验收：原文备份
- Word Online 首次选中部分文字翻译，等待备份保存后应成功替换，不误报“原文已更改”；已有备份时再次翻译也应成功。段落、全文分别复测。
- 翻译等待期间编辑或删除选区文字（包括只改空格），确认取消替换；仅切换选区时仍替换最初选中的范围。仅格式或图片变化不在文字冲突检查范围内。
- 分别从选区、段落、正文全文开始翻译，确认恢复按钮均还原首次翻译前的整份正文。
- 多次翻译后保存文件，关闭 Word 并重新打开，点击“恢复原文”并确认，检查正文、表格、图片、链接与格式；重点验收大图片及复杂 OOXML 文档的备份容量和还原效果。
- 恢复前编辑正文，确认提示说明将撤回编辑；点击取消不改变内容，确认后整份正文恢复。页眉、页脚等独立内容不受影响。
- 没有备份时点击恢复显示明确错误；保存备份失败时不替换正文。恢复失败后备份继续保留，允许重试。
- 恢复不需要登录或翻译服务；始终翻译模式下，恢复后当前面板不立即再次自动翻译。
- 保存带备份的文档后在同一加载项中重新打开，确认备份随文档保留，后续翻译及重复恢复不会覆盖首次原文。
