# Word 翻译面板设计验证

final result: passed

## 参考与实现

- 选区页参考：`C:/Users/nalus/AppData/Local/Temp/codex-clipboard-cb4fd468-d93a-49c4-9fb2-d977559a6b55.png`（1024 × 1536）。
- 全文页参考：`C:/Users/nalus/AppData/Local/Temp/codex-clipboard-1f51245c-8ea9-4ad9-912c-f36652920c53.png`（477 × 716）。
- 异常参考：`C:/Users/nalus/AppData/Local/Temp/codex-clipboard-e950b4b4-487e-42a5-abbd-ed870dede0ba.png`；依用户要求，仅保留底部统一提示，不重复显示译文下方警告。
- 实现：`http://localhost:4173/word/taskpane.html`。
- 浏览器：Codex 内置浏览器；420 × 900 CSS px，截图 420 × 900 px（1:1）；窄面板 320 × 900 CSS px，截图 320 × 900 px。
- 截图：`docs/ui-qa/selection.png`、`document.png`、`narrow.png`、`repair.png`。repair 为 fullPage API 输出（404 × 987 px），用于补充验证底部提示，不作为精确尺寸比较。
- 比较时排除参考图的外围空白、阴影和 Word 原生标题栏，按内容面板宽度归一化。生产页面不重复绘制 Word 原生标题栏。全文页按用户要求保留顶部三个范围按钮。
- 原文示例实际为 5 个字符，计数使用真实内容，不复制参考图中不一致的“28 个字符”。

## 全页和局部比较

参考图与实现截图已在同一个图像输出中共同打开。对照选区页的标题、三个范围选项、语言字段、原文框、浅蓝译文框、底部操作和绿色提示；对照全文页的全文翻译、目标语言和保存设置三张卡片。局部检查选中背景、按钮、通知颜色以及长文案换行。参考图按比例适配 Office 面板，输入区保留可调整高度。

## 比较记录

1. 初次比较发现选中按钮的浅蓝背景被共享 hover 样式覆盖。修复 Word 专用样式优先级，重新捕获 selection.png / document.png 并再次与源图共同检查，已修复。
2. 异常提示标题被共享 error 样式染红，修复为继承警告卡片的棕色，重新捕获 repair.png，标题与警告图标颜色一致。
3. 320px 窄面板实测 clientWidth / scrollWidth 均为 320，范围按钮、文本框与底部操作未发生横向溢出。

无剩余 P0/P1/P2 视觉问题。P3：现有品牌图标与参考图存在细微差别；浏览器原生下拉箭头随 Word WebView/系统版本变化。

## 交互验证与边界

- 浏览器实测：选中内容读取与模拟翻译、全文切换、更多操作及全选译文、设置保存、恢复确认打开与取消、底部提示关闭、模拟控件异常。
- 237 项 Word 单元测试通过；最后的状态重试修改后，相关 26 项面板测试再次通过。
- 类型检查和 Word 生产构建通过。构建仍有项目原有大图片体积提示。
- 控制台检查：初次预览模拟宿主脚本出现语法错误，已修复；重新加载后的交互无新增 JavaScript 错误。
- 浏览器使用 scripts/word-ui-preview.cjs 模拟 Office、账户和翻译服务。该脚本仅监听本机回环地址，不进入生产构建。它不验证真实 Word 写入、恢复、清除控件、SSO 或全文副本创建；这些需要 Word 客户端集成验证。
- 修复复用既有清除翻译控件逻辑：保留文字和格式，但清除后这些范围无法再通过“恢复原文”还原。界面在执行前明确说明，只有点击“立即修复”才运行。

复现：先运行 `npm run build:word`，再运行 `node scripts/word-ui-preview.cjs`。原文输入“模拟控件异常”可查看异常提示。


## 后续调整：紧凑布局与账户头像

依据用户后续要求，缩小整体字号并压缩间距，以新的紧凑度要求替代原图的大字号。标题 20px，主要标签/控件 12px，翻译正文 13px；缩小页边距、字段间距和下拉框高度，原文最小高度 88px、译文最小高度 112px，仍可拖动调整。

右上角使用现有账户头像组件，支持用户照片及姓名首字回退，直接打开包含重新登录、注销的账户菜单。文档维护及备份说明收纳到全文页底部折叠区。

截图：docs/ui-qa/compact-selection.png、docs/ui-qa/compact-account.png（406 × 850 CSS px / PNG px）。选区、译文、底部按钮和成功提示可同时显示。另测 320 × 720：页面宽度与滚动宽度均为 320px，底部操作按钮下缘为 611px，无横向溢出。

浏览器模拟宿主中验证：头像菜单打开、注销后禁用翻译、重新登录恢复翻译、Escape 关闭菜单；控制台无错误。26 项面板测试、类型检查及 Word 构建通过。真实照片来自既有账户接口，真实 SSO 仍需 Word 客户端验证。

本次调整 final result: passed
